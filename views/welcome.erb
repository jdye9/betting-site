<!DOCTYPE html>
<html>
    <head>
        <link href="/stylesheets/welcome.css" rel="stylesheet" type="text/css"/>
    </head>
    <body>
        <div class="welcome">
            <div class="welcome-header">
                Welcome!
            </div>
            <p>
                <a class="welcome-link" href="/login">log in</a> to gamble.<br><br>
                <a class="welcome-link" href="/signup">sign up</a> to create account.
            </p>
        <% if session[:signupsuccess] %>
            <p style="color: green; font-family: 'Sniglet', cursive; text-align: center; font-size: 20px;"><%= session[:signupsuccess] %></p>
            <% session[:signupsuccess] = nil %>
        <% end %>
        </div>
    </body>
</html>