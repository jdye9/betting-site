<!DOCTYPE html>
<html>
    <head>
        <link href="/stylesheets/signup-login.css" rel="stylesheet" type="text/css"/>
    </head>
    <body>
        <div class="signup-login">
            <div class="signup-login-header">
                Log In!
            </div>
            <form action="/login" autocomplete="off" method="post">
                username: <input type="text" name="username" required><br>
                password: <input type="password" name="password" required><br>
                <input class="submit" type="submit" name="signup" value="Log In">
            </form>
            <% if session[:loginerror] %>
                <p style="color: red; font-family: 'Sniglet', cursive; text-align: center; font-size: 20px;"><%= session[:loginerror] %></p>
                <% session[:loginerror] = nil %>
            <% end %>
        </div>
    </body>
</html>