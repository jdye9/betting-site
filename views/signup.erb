<!DOCTYPE html>
<html>
    <head>
        <link href="/stylesheets/signup-login.css" rel="stylesheet" type="text/css"/>
    </head>
    <body>
        <div class="signup-login">
            <div class="signup-login-header">
                Sign Up!
            </div>
            <form action="/signup" autocomplete="off" method="post">
                username: <input type="text" name="username" required><br>
                password: <input type="password" name="password" required><br>
                <input class="submit" type="submit" name="signup" value="Sign Up">
            </form>
            <% if session[:signuperror] %>
                <p style="color: red; font-family: 'Sniglet', cursive; text-align: center; font-size: 20px;"><%= session[:signuperror] %></p>
                <% session[:signuperror] = nil %>
            <% end %>
        </div>
    </body>
</html>